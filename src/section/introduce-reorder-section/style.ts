import styled from 'styled-components';
import { ExampleContainer } from '../section-styles';

export const CustomExampleContainer = styled(ExampleContainer)`
    flex-direction: column;
    position: relative;
`;
